# Application Configuration
PORT=3000
NODE_ENV=development
BASE_URL=http://localhost:3000
# For production, use your domain or hosting URL
# BASE_URL=https://your-app-domain.com

# MongoDB Connection (MongoDB Atlas or local MongoDB)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/url-shortener?retryWrites=true&w=majority
# For local MongoDB use: mongodb://localhost:27017/url-shortener

# Session Secret (Change this to a secure random string)
SESSION_SECRET=your_secret_session_key_change_this

# Admin User Setup (First user with these credentials will be admin)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=adminpassword123

# Rate Limiting (requests per 15 minutes window)
RATE_LIMIT=100